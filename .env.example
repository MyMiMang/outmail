# 复制为 .env 后修改
# 共享访问密码：留空=不要密码（仅推荐内网用），公网部署务必填一个强密码
ACCESS_PASSWORD=

# CORS（一般同源部署不用改）
ALLOWED_ORIGINS=*

# 部署域名：填了即自动 HTTPS（Caddy + Let's Encrypt）
# 留空或 :80 表示仅 HTTP
DOMAIN=:80

# Let's Encrypt 通知邮箱（可选）
EMAIL=
